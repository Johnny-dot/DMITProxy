# Security / 安全说明

Prism 会接触上游管理凭据、订阅 ID、用户账户和流量信息。演示模式使用公开的假账号，仅供本地运行，不应作为公网演示服务部署。

## 报告安全问题

请使用仓库 Security 页的 **Report a vulnerability** 私下报告；若入口不可用，请先联系维护者约定私密渠道，不要在公开 issue 中上传真实凭据、可用订阅或生产日志。

报告包含受影响版本、配置条件、最小复现、影响及建议。尽量使用假账号、临时数据库和模拟上游。

## 部署要求与支持范围

- 使用维护中的 Node 版本及已更新的锁文件。
- 通过 HTTPS 反向代理暴露服务，保护上游面板和服务账号。
- `VITE_*` 是浏览器可见配置，不得存放秘密。
- `.env`、数据库、日志、备份、客户端配置应留在 Git 之外。
- 依赖审计、模拟测试和截图不能代替具体部署的安全评估。

当前改进计划见[架构文档](docs/ARCHITECTURE.md#后续改进)。依赖修复优先落在默认分支；尚无长期支持旧分支。
